# コンペ概要

対象は Kaggle の Store Sales - Time Series Forecasting です。目的は、店舗 `store_nbr` と商品カテゴリ `family` ごとの将来売上 `sales` を予測することです。

評価指標は RMSLE です。売上は右裾が長く、ゼロ付近も多いため、対数誤差との相性が良い問題設定です。

今回のワークスペースでは、まず最小ベースラインを提出し、その後にローカル CV を整備して改善案を比較しました。最終的には、曜日と販促を使った階層平均モデルを採用しています。
